\section{Ejemplos para generar índices}

% --- Ejemplo de Figura ---
\section{Diseño del Sistema (Figuras)}
Como se observa en la Figura \ref{FIG:LOGO}, el diseño es modular.
\begin{figure}[h]
    \centering
    % Asegúrate de tener logoUAM.png en la carpeta img/
    \includegraphics[width=0.3\textwidth]{logoUAM} 
    \caption{Logotipo de la UAM como ejemplo de figura.}
    \label{FIG:LOGO}
\end{figure}

% --- Ejemplo de Ecuación ---
\section{Modelo Matemático (Ecuaciones)}
Para el cálculo de rendimiento utilizamos la Ecuación \ref{EQ:EINSTEIN}:
\begin{equation}
    E = mc^2
    \label{EQ:EINSTEIN}
\end{equation}

% --- Ejemplo de Código ---
\section{Implementación (Códigos)}
El siguiente listado muestra la conexión principal.
% Asegúrate de que la carpeta 'codes' existe o pon el código directo así:
\begin{lstlisting}[language=Python, caption={Script de conexión}, label={COD:CONEXION}]
def conectar():
    print("Conectando al servidor...")
    return True
\end{lstlisting}

% --- Ejemplo de Algoritmo ---
\section{Algoritmia (Algoritmos)}
El procedimiento de ordenación se detalla en el Algoritmo \ref{ALG:BURBUJA}.
\begin{algorithm}[H]
    \SetAlgoLined
    \KwResult{Lista ordenada}
    \While{no ordenado}{
        intercambiar elementos\;
    }
    \caption{Algoritmo de ordenación simple}
    \label{ALG:BURBUJA}
\end{algorithm}

% --- Ejemplo de Tabla ---
\section{Resultados (Tablas)}
La Tabla \ref{TAB:RESULTADOS} muestra la comparativa.
\begin{table}[h]
    \centering
    \begin{tabular}{|c|c|}
        \hline
        \textbf{Caso} & \textbf{Tiempo (ms)} \\
        \hline
        Caso A & 15 \\
        Caso B & 20 \\
        \hline
    \end{tabular}
    \caption{Tiempos de ejecución obtenidos.}
    \label{TAB:RESULTADOS}
\end{table}