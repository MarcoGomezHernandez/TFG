Dado el .md en el que se describe un escalado de resolucion en el eje temporal y un factor y offset de escalado del rango del eje vertical, quiero que hagas el fichero c++ y su .h (que te paso) que disponga de una función externa para realizar esto de la siguiente forma:

Esta función externa recibirá todo lo que necesite (igual que lo descito en el md que es introducido externamente y no se calcula dentro), y además ahora, la ruta a un csv (sin titulos) que tendrá los voltajes a leer de la neurona viva, en lugar de recibirlos de un DAQ. Que se te pase también el paso de ese csv y el tiempo de medición (en base a ese paso). Además, para poder escalar sólo parte del csv, que se incluya el tiempo de inicio (en referencia al paso, de nuevo), y el tiempo a usar (también, en base al paso, como todos los tiempos). Es decir, del csv se coge sólo un fagmento, y dentro de ese fragmento está el tiempo de observación, y el total que se escala con ese tiempo de observación. Además, que se incluya un argumento diciendo el indice de la columna a leer del csv; junto con un argumento que indique el integrador a usar (de momento sólo poner RK4).

Que la función retorne un array, o vector, o lo que quieras, que contenga los valores de este csv escaldos tal y como se hace en el md que te paso, pero además, haciendo una interpolación lineal (añadiendo valores entre los puntos originales) de tantos puntos intermedios (entre los dos puntos originales) como pasos daría la neurona modelo entre esos dos puntos, como nos diga el algoritmo de ajuste de la resolución correspondiente (s_points); y además que retorne el paso de integración que debe usar el modelo.

Las nueronas modelo a usar serán hindmarsh rose, así que que usa sus máximos, mínimos, tablas precomputadas (aparecen en el propio md)... Puedes poner esto como constantes, estructuras o lo que más convenga.

Trata de que todo se haga lo más parecido al md dentro de lo posible (mantendiendo nombres de variables, de funciones, estructura del código, que hagan lo mismo...). Que no se te olvide esta cosa que tiene el md del check de drift, y que se pueda especificar el el check_drift (drift_n_burst fíjalo a 2). Haz la adaptación a c++ de la manera lo más parecida y adecuada posible.

Ten en cuenta que esta inetrpolación de la neurona del csv y retorno de dt es el escalado horizontal, y el escalado vertical de la neurona del csv, es el verical (ya sea sólo con el tiempo de observación, o tambien con el chequeo de drift). Para facilitar la interpolación, haz primero el escaldo de voltajes y luego interpolas estos.