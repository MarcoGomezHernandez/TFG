Quiero que me hagas un algoritmo genético en la función del hilo de la versión online de la misma forma que se hace e la verión offline (genetic.tpp). Este se hará usando la función de fitness de la versión online, y de una forma lo más parecida posible a la versión offline. Te dejo el fichero base que debes modificar, pero las cosas que se calculan para pasar a calc_fitnesses deben ser calculadas antes del bucle genético, y de la misma forma (salvo la inicializacion de parametros, que es un ejemplo). Además, ahora quiero que saques los rangos de inicializacion y de variacion de los parametros (así como esín, que se queda fijo) de las constantes, y ahora estarán en una estructura y no serán constantes. Esto es así porque estos rangos serán distintos en cada ejecución y dependerán de varias fórmulas que tu de momento no debes implementar (deja un comentario indicando que ahí falta eso); además de porque los rangos de la sinapsis de cada dirección serán distintos.

Hay muchas cosas en la versión offline que no seon necesarias aqui, así que no hace falta que implementes absolutamente todo. Además, si haces funciones que se usarán una vez en la funcion externa y con los mismos argumentos en cada ejecucion de la funcion externa, puedes añadirla a la clase del modulo si es que necesita leer sus atributos en vez de pasarlos como argumentos.

Online: @beautifulMention@beautifulMention@beautifulMention 
Offline: @beautifulMention@beautifulMention @beautifulMention 

sslow y fast no necesitan tener en cuenta offset, porque eso lo tiene vslow; solo escala

esyn hay que settearlo en el alg genético, no en el modulo.

max vfast y vslow: maxpre+((maxpre-minpre)/7)
min vfast y vslow: minpre-((maxpre-minpre)/7)

min sfast: 4.12/(maxpre-minpre)     max sfast: 13.72/(maxpre-minpre)
min sslow: 1.72/(maxpre-minpre)     max sslow: 3.43/(maxpre-minpre)

esyn antiphase: maxpost+(3.86*(maxpost-minpost))      esyn phase: minpost-(3.86*(maxpost-minpost))

k1 y k1 min: 0.007/dt      k1 y k2 max: 70/dt

gfast min: 0.001     gfast max: 5.0
